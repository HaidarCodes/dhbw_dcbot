{
  "name": "dhbw-discord-bot",
  "private": true,
  "version": "1.0.0",
  "description": "Discord category management for the DHBW Rapla schedule",
  "main": "app.js",
  "type": "module",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "start": "node app.js",
    "register": "node commands.js",
    "dev": "nodemon app.js",
    "check": "node --check app.js && node --check archive-sync.js && node --check commands.js && node --check state-store.js && node --check utils.js",
    "test": "node --test"
  },
  "license": "MIT",
  "dependencies": {
    "discord-interactions": "^4.0.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
